language: c
compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - libc6-dev

script:
  - mkdir build && cd build
  - cmake .. -DENABLE_COVERAGE=On -DENABLE_TESTS=On
  - make
  - make test

after_success:
  - make gcov
  - bash <(curl -s https://codecov.io/bash)
